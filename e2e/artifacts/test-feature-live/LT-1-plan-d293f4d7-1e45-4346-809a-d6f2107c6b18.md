# LT-1 — run d293f4d7-1e45-4346-809a-d6f2107c6b18 (recon)

POST body:
```json
{
 "problem": "Recon: survey the target and propose a test plan \u2014 the scenarios, their dependencies, and which are deterministic tool checks vs governed agent runs. Present the proposed plan at the intake gate and launch nothing until it is approved.\n\nSurvey wicked-studio at its current main and propose a test plan covering its live /ws CoreEvent fold (awaitingHuman, unitPlanned, sessionCompleted frames), the /api/v1 routes it calls (runs, campaigns, testing/recon, projects, repos), its CLI entry points, and its UI pages (/testing/campaigns, /runs/:id, /steering, the home deck), naming the real source files behind each scenario and classifying each as a deterministic tool check or a governed agent run",
 "repoRefs": [
  "wicked-studio"
 ]
}
```

## unit 1

_verbatim from `GET /runs/:id/units/1/output` — 4739 chars_

I'll survey the repo structure, recent changes, and existing test tooling to build the test plan.## Survey

**Target**: wicked-studio v0.5.1 on `main` (this worktree, clean). Recent ship wave: DES-HOME-COMMAND-CENTER (command-deck landing rebuild), nav reorg (Make→Execute/Vibe/Demo, Test→Evals), campaign→test rename, deck-styled section dashboards, Memories stats band.

**Baseline (ran just now)**:
- `npm test` → 237 files / 2442 tests green
- `npm run lint` / `npm run typecheck` → clean
- CI (`ci.yml`) = lint · typecheck · test · build, no daemon, no crew checkout — deliberately decoupled (RAID R-01/R-02)
- A live `wicked-crew` daemon **is** reachable right now at `:7701` (v0.7.25) — and it's tracking this very session as a run (`/runs` lists this worktree's id). Read-only checks against it are actually possible today, not hypothetical.

**Key gap found**: `e2e/studio_standalone_test.py` (the one operator-run functional gate, per TEST-001 §4) still asserts against the **pre-rebuild** surface — DES-MERGE-001 slices (project shell, orchestrator board, old `band-needs-you` wall). It has no assertions for the new Home command-deck (needs-you queue, KPI band, essence strip, recent-activity), the Execute/Vibe/Demo top-level rail, or the campaign→test rename. Unit coverage for the new surface is solid (DES-HOME-COMMAND-CENTER §8 lists it, and it's in the green 2442); the live-daemon proof hasn't caught up.

## Test plan

| # | Scenario | Depends on | Class |
|---|---|---|---|
| 1 | Needs-you queue: severity order, dedupe (onboard-suppression, campaign-subtraction), windowing | none (pure fold) | **Deterministic** — `needsYou.test.ts` ✅ covered |
| 2 | Contradiction guard: 21 failed runs ⇒ calm copy cannot render | none | **Deterministic** — `HomeBoard.queue.test.tsx` ✅ covered |
| 3 | KPI band folds/thresholds/deltas, essence-strip omission on absent wires, empty-portfolio state | none | **Deterministic** — `HomeBoard.command.test.tsx` ✅ covered |
| 4 | Recent-activity narration reuse/ordering/cap | none | **Deterministic** — `homeActivity.test.ts` ✅ covered |
| 5 | Nav rename: Execute/Vibe/Demo routes, redirects (`/make`→`/execute` bare-only), mutually-exclusive Vibe/Demo pickers, `/testing`→Evals redirect | none | **Deterministic** — `LeftSidebar.rail.test.tsx`, `MadeDashboard.test.tsx`, `notFoundRoute`/`testingRoutes.test.tsx` ✅ covered |
| 6 | Campaign→Test rename: copy, constant rename, project-scoped auto-select | none | **Deterministic** — `TestingLaunch.test.tsx` ✅ covered |
| 7 | Memories stats band correctness on absent/present store coverage | none for the fallback logic | **Deterministic** — component test; ⚠️ *real* store-size semantics only provable live (see #10) |
| 8 | 1440×700 command-center layout budget (queue + KPI both visible, no scroll) — currently only "verified by live screenshot" in the design doc, no automated assertion | headless browser + real viewport | **Gap** — needs a Playwright viewport screenshot/height assertion; not unit-testable in jsdom |
| 9 | Wire-contract drift: studio's pinned `wicked-crew-api-types` vs the live daemon's actual response shapes | live daemon (`:7701` available now) | **Live read-only smoke** — `curl`/typed fetch against `/api/v1/diagnostics`, `/runs`, `/campaigns`, `/governance/*` and diff against the pinned type version (closes RAID I-01's staleness risk) |
| 10 | Full standalone e2e golden path (launch → connect → list/open run → approve gate → live WS narration → evidence) | crew daemon binary, Playwright chromium, `e2e/studio_standalone_test.py` | **Operator-run e2e** (per TEST-001 §4) — **stale**, needs new assertions added for queue/KPI band/essence strip/Execute-Vibe-Demo/Evals before it can back the next release's `verdict.json` |
| 11 | Live governed-run observation: spawn a real run through the daemon, watch the Home board's queue re-sort on gate arrival, KPI deltas update within the 2s budget, a narration line land — proving the command-deck is actually live, not just fixture-fed | a live multi-CLI council run (claude/codex/pi/opencode/copilot spin-up) | **Governed agent run** — heaviest/spikiest tier (ops playbook: serialize, never run concurrently); pre-release smoke only, not per-PR |

## Recommendation

Run tiers 1–7 now (already green, no action needed). Do #9 as a cheap live check before the next release to retire RAID I-01. Prioritize #10 — updating the standalone e2e for the shipped command-deck/nav surface — before the next tagged release, since TEST-001's evidence protocol requires a green operator-run e2e dated within 7 days of the release tag, and the current script doesn't exercise what actually shipped. #11 (a true governed run) is optional confirmation, not a release gate.

## unit 2

_verbatim from `GET /runs/:id/units/2/output` — 8767 chars_

pi v0.84.2
---

## Skills
- … (54 local toolchain paths elided)

## Extensions
- … (1 local toolchain path elided)
I'll start by orienting in the actual worktree and confirming the baseline the prior phase reported.Daemon is live (200). Let me confirm the deterministic baseline (tiers 1–7) and inspect the stale e2e script that the plan flagged as the priority gap (#10).Typecheck + lint clean. Now the full deterministic suite (tiers 1–7) and the live read-only wire-contract smoke (#9), since the daemon is up.Suite green (237 files / 2442 tests). Daemon: crew 0.7.25, studioBundle 0.5.1 (matches this repo), studio pins `wicked-crew-api-types ^0.25.0`. Now the read-only endpoint smoke (#9) against the surfaces the plan named.Key finding: daemon serves `/campaigns` (200) but not `/tests` or `/evals` (404) — so the studio-side campaign→test / Test→Evals rename must be **presentational only**. Let me confirm the API client still calls the real wire paths.The rename is confirmed UI-only — client still calls real wire paths (`/campaigns`, `/testing/evals`). My bare `/evals` probe correctly 404'd because the real path is `/testing/evals`. Let me verify those actual paths respond.Let me confirm `/testing/campaigns` is a studio front-end route (not an API call) so its 404 isn't drift — and check the eval shape against the pinned types.All runnable tiers of the plan are now executed. Summary below.

## Execution verdict — wicked-studio v0.5.1 test plan

**Environment**: this worktree (clean, `main` @ `b30000f`), live crew daemon `:7701` (crew 0.7.25, studioBundle 0.5.1 — matches).

### Deterministic baseline — tiers 1–7 ✅ PASS
| Check | Result |
|---|---|
| `npm run typecheck` | clean |
| `npm run lint` | clean |
| `npm test` | **237 files / 2442 tests green** (69s) |

Covers needs-you queue fold, contradiction guard, KPI band, recent-activity, nav rename (Execute/Vibe/Demo + redirects), campaign→test rename, Memories band — exactly the tiers the plan marked already-green.

### Tier 9 — live read-only wire-contract smoke ✅ PASS (RAID I-01 retired)
- Pinned `wicked-crew-api-types` **0.25.0** == installed **0.25.0** — no version drift.
- `GET /runs` `{runs:[{session,units}]}`, `/projects`, `/campaigns` `{campaigns,groups}`, `/governance/policies`, `/testing/evals` — **all 200**, shapes match the pinned types.
- `EvalRunSummary` live keys `(id, created_at, actor, corpus, type_filter, rule_store, summary, per_type, degraded)` match the pinned interface **field-for-field**.
- **Key confirmation**: the campaign→test / Test→Evals renames are **presentational only** — the client still calls the real wire paths (`/campaigns`, `/testing/evals`). `/tests`, `/evals` (bare), and `/testing/campaigns` 404 correctly because none are API paths studio calls; `/testing/campaigns` is a front-end route (grep confirms zero `apiFetch` to it).

### Outstanding gaps — tiers 8, 10, 11 (unchanged, not runnable here)
- **#8** 1440×700 layout budget — needs Playwright viewport assertion; not jsdom-testable.
- **#10** `e2e/studio_standalone_test.py` (3151 lines) still asserts the **pre-rebuild** DES-MERGE-001 surface (project shell, orchestrator board, `band-needs-you`). No assertions for the shipped Home command-deck / Execute-Vibe-Demo rail / Evals. **This remains the priority before the next tagged release** — TEST-001 §4 requires a green operator-run e2e dated within 7 days of the tag, and the current script doesn't exercise what shipped. Updating it is a substantive, separate change (operator env: crew binary + Playwright chromium), not executable non-destructively in this phase.
- **#11** governed live run — optional pre-release confirmation, not a gate.

**Bottom line**: everything runnable without an operator/Playwright rig is **GREEN**, and the live daemon smoke closes the wire-contract staleness risk. The one carried-forward blocker for release evidence is refreshing the standalone e2e (#10).

No external payload-transforming third-party services are involved in these checks, so no `ASSUMPTION[external-transform]` lines apply.

## unit 3

_verbatim from `GET /runs/:id/units/3/output` — 7319 chars_

Good, this confirms enough for the file-level test plan. Now writing it up.## Survey: source-level map

**No CLI bin** — `package.json` has no `bin` field (studio is a pure SPA). "Entry points" here means npm scripts (`dev`/`build`/`typecheck`/`lint`/`test`/`prepare`) plus standalone node scripts in `scripts/` (`generate-testid-inventory.mjs`, `prepare-dist.mjs`, `changelog-section.py`).

**WS fold**: `src/hooks/useEventStream.ts` opens `new WebSocket(\`${wsBase()}/ws\`)` (built from `src/api/client.ts`'s `wsBase()`), parses tagged-JSON `CoreEvent` frames, reconnects on close (3s backoff), and hands events untyped to callers — the switch/fold lives downstream in three consumers: `src/store/runtime.ts` (`unitPlanned` line 58, `awaitingHuman` line 91, `sessionCompleted` line 99 — council/unit UI state), `src/store/gates.ts` (`awaitingHuman` opens a gate line 137, `sessionCompleted` prunes line 159 — the gate cache reconciling with `GET /runs`), `src/components/narrator.ts` (`unitPlanned`/`awaitingHuman`/`sessionCompleted` → human narration strings feeding `NarratorFeed.tsx` and the Home board's recent-activity strip).

**REST routes** (all in `src/api/client.ts` + per-domain files): `/runs`, `/runs/:id`, `/runs/:id/{events,acceptance,archive,gate,cancel,guidance,resume,units/:ord/output,elicitation,files,diff,inject,deliver}` (client.ts); `/campaigns`, `/campaigns/:id`, `/runs/:id/acceptance` (`src/api/campaigns.ts`); `/testing/recon`, `/testing/evals[/run|/:id]`, `/testing/corpora/import` (`src/api/testing.ts`); `/projects[/:id[/members|/prompts|/activity]]` (client.ts); `/repos[/:id[/onboard|/requirements|/graph|/git-history|/contributors|/domain-graph]]` (client.ts); `/steering/{dashboard,policies,memories}` (`src/api/steering.ts`).

**UI pages**: `/testing/campaigns` → `CampaignsPage.tsx` (+`CampaignScoreboard.tsx`, `TestingLaunchPanel.tsx`); `/runs/:id` → **no dedicated page** — `useLegacyRedirect.ts` folds it into the project shell (DES-MERGE-001), rendered by `ProjectDashboard.tsx`/`MadeDashboard.tsx` + `RunTimeline.tsx`/`RunsSection.tsx`; `/steering` → `SteeringPage.tsx` (+`SteeringGrid.tsx`, `SteeringUsageBand.tsx`, `GovernanceDashboard.tsx`); home deck → `HomeBoard.tsx`/`HomeCommand.tsx` (+`dashboardKit.tsx`, `DashboardTiles.tsx`).

## Test plan

| # | Scenario | Real source | Dependencies | Class |
|---|---|---|---|---|
| 1 | WS reconnect/backoff, stale-socket guard, malformed-frame skip | `useEventStream.ts` | mock `WebSocket` | **Deterministic** — no dedicated `useEventStream.test.ts` found; **gap**, recommend adding one (currently only exercised indirectly via consumers) |
| 2 | `awaitingHuman` opens a gate, `sessionCompleted` prunes it, cache reconciles with `GET /runs` | `store/gates.ts` | none | **Deterministic** — `gates.store.test.ts`, `batchGates.test.ts` ✅ |
| 3 | `unitPlanned` records executor type for council UI; `awaitingHuman`/`sessionCompleted` runtime transitions | `store/runtime.ts` | none | **Deterministic** — `runtime.store.test.ts` ✅ |
| 4 | Narration strings for `unitPlanned`/`awaitingHuman`/`sessionCompleted` | `components/narrator.ts` | none | **Deterministic** — covered indirectly via `homeActivity.test.ts`/`HomeBoard.command.test.tsx` (survey phase 1); no direct `narrator.test.ts` found — **gap** for exhaustive per-type string assertions |
| 5 | `GET/POST /runs` + subresources (archive, gate, cancel, guidance, resume, deliver, inject) — request shape, error handling | `api/client.ts` | none (mocked fetch) | **Deterministic** — unit-testable per TEST-001 §1 |
| 6 | `/campaigns` 404-tolerant probe (older daemon has no route) | `api/campaigns.ts` §1.5 comment | none | **Deterministic** — should have a unit test asserting graceful degrade on 404; verify it exists (`tests/campaignsApi.test.ts` or similar) |
| 7 | `/testing/recon`, `/testing/evals*`, `/testing/corpora/import` | `api/testing.ts` | none | **Deterministic** — `TestingLaunch.test.tsx` covers recon launch; evals covered per phase-1 report |
| 8 | `/repos*` incl. onboard/graph/blast-radius/domain-graph | `api/client.ts` | none | **Deterministic** — mocked-fetch unit tests |
| 9 | `/steering/{dashboard,policies,memories}` + legacy `/steering/:type` redirect | `api/steering.ts`, `useSteeringRedirect` (App.tsx) | none | **Deterministic** — `SteeringPage.test.tsx`, `steeringApi.test.ts` ✅ |
| 10 | `/runs/:id` legacy bookmark → project-shell redirect | `useLegacyRedirect.ts` | none | **Deterministic** — covered per phase-1 (`notFoundRoute`/nav tests) |
| 11 | CampaignsPage UI: launch, scoreboard render, project auto-scope | `CampaignsPage.tsx`, `CampaignScoreboard.tsx` | none (RTL + mocked API) | **Deterministic** |
| 12 | Run detail rendering (timeline, output, gate chip, diff) inside the project shell | `RunTimeline.tsx`, `RunsSection.tsx`, `ProjectDashboard.tsx` | none | **Deterministic** — component tests |
| 13 | SteeringPage dashboard/grid/usage band | `SteeringPage.tsx`, `SteeringGrid.tsx`, `SteeringUsageBand.tsx` | none | **Deterministic** |
| 14 | Home deck: KPI band, needs-you queue, recent activity (covered in phase 1) | `HomeBoard.tsx`, `HomeCommand.tsx`, `board/needsYou.ts` | none | **Deterministic** ✅ (phase-1 result) |
| 15 | Real WS frame shapes vs `CoreEvent` type union — does a live daemon ever send a `type` the fold doesn't switch on (additive-safe by design, but confirm nothing *silently* misfolds) | `useEventStream.ts` design comment (§5.1) + `api/types.ts` `CoreEvent` union | live daemon `:7701`, actually observe frames on `/ws` during a run | **Live read-only smoke** — attach a WS client, log `type` values for a few minutes against the daemon's live run, diff against the union in `types.ts` |
| 16 | End-to-end: connect → `GET /runs` reconcile → live `awaitingHuman` frame opens gate UI → approve → `sessionCompleted` frame closes it → narration/timeline update | `useEventStream.ts` + `gates.ts` + `narrator.ts` + `RunTimeline.tsx`, driven by `e2e/studio_standalone_test.py` | live crew daemon, Playwright chromium | **Operator-run e2e** — same script flagged stale in phase 1; still doesn't assert the post-rebuild Home deck/nav, but its WS-gate-approval flow (steps 5–9 in its docstring) is the one existing proof of this exact fold and should stay the backbone once refreshed |
| 17 | A real governed run (multi-CLI council) producing genuine `unitPlanned`→`awaitingHuman`→`sessionCompleted` frames end to end, observed live on the Home deck | whole fold, driven against `:7701` | a live governed run (heavy, spiky per ops playbook) | **Governed agent run** — pre-release confirmation only, not per-PR; the daemon at `:7701` already has one such run in flight (this session) that could serve as the read-only subject for #15 without spawning a new one |

**Priority gaps to close**: #1 and #4 have no dedicated unit test file (fold correctness is only asserted indirectly through consumers) — cheap to add and they're the actual WS-parsing/narration logic named in this phase's brief. #16's script refresh (carried from phase 1) remains the standing release blocker.

No third-party payload-transforming service is involved anywhere in this fold — every transform (`CoreEvent` → narration, → gate state, → KPI folds) is first-party code in this repo, so no `ASSUMPTION[external-transform]` lines apply.
